{
  "name": "fitness-project",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "fitness-app-frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace fitness-app-frontend",
    "build": "npm run build --workspace fitness-app-frontend",
    "lint": "npm run lint --workspace fitness-app-frontend",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  }
}
